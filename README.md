# Kleos

Kleos is a public-signup SaaS for automated job outreach: scrape job boards, find recruiter emails, generate tailored cold email drafts, and send through each user's own SMTP account.

The public dashboard is served from `/kleos/`; the API is served from `/kleos/api/`.
